name: Daily Super-Duper Bot

on: 
  push:


env:
    API_KEY: ${{ secrets.API_KEY }}
    

jobs:
    Daily-Run:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: Run App
              run: python app.py